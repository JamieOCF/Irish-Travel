-- -- Manually set an account to be an admin account, used for creating first admin account
-- UPDATE users SET is_admin = 1 WHERE user_id = '';

-- -- Remove all users from users table
-- DELETE FROM users;

-- -- Inspect users table in DB
-- SELECT * FROM users